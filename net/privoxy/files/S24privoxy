#!/bin/sh

ENABLED=yes

DESC="Privoxy server"
PROCS="privoxy"
PROG="/opt/sbin/$PROCS"

PID_FILE="/opt/var/run/$PROCS.pid"

start_pre() {
  rm -f "PID_FILE"
}

PRECMD="start_pre"
ARGS="--pidfile $PID_FILE /opt/etc/$PROCS/config"
PREARGS=""

. /opt/etc/init.d/rc.func
