--- a/syscall.c
+++ b/syscall.c
@@ -33,9 +33,16 @@
 #include <sys/syscall.h>
 #endif
 
+/* SYS_openat2 | openat2.h since Linux 5.6 */
+#ifndef LINUX_VERSION_CODE
+#include <linux/version.h>
+#endif
+
 #ifdef __linux__
 #include <sys/syscall.h>
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5,6,0)
 #include <linux/openat2.h>
+#endif /* openat2.h since Linux 5.6 */
 #endif
 
 #include "ifuncs.h"
@@ -1692,6 +1699,7 @@ static int path_has_dotdot_component(con
 }
 
 #ifdef __linux__
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5,6,0)
 static int secure_relative_open_linux(const char *basedir, const char *relpath, int flags, mode_t mode)
 {
 	struct open_how how;
@@ -1731,6 +1739,7 @@ static int secure_relative_open_linux(co
 		close(dirfd);
 	return retfd;
 }
+#endif /* SYS_openat2 since Linux 5.6 */
 #endif
 
 #ifdef O_RESOLVE_BENEATH
@@ -1792,6 +1801,7 @@ int secure_relative_open(const char *bas
 	}
 
 #ifdef __linux__
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5,6,0)
 	{
 		int fd = secure_relative_open_linux(basedir, relpath, flags, mode);
 		/* ENOSYS = kernel < 5.6 doesn't have the syscall even though
@@ -1799,6 +1809,7 @@ int secure_relative_open(const char *bas
 		if (fd != -1 || errno != ENOSYS)
 			return fd;
 	}
+#endif /* openat2 since Linux 5.6 */
 #endif
 
 #ifdef O_RESOLVE_BENEATH
