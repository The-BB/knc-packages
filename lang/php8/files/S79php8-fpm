#!/bin/sh

ENABLED=yes

DESC="PHP FPM"
PROCS="php8-fpm"
PROG="/opt/bin/$PROCS"

PID_FILE="/opt/var/run/$PROCS.pid"

start_pre() {
  rm -f "$PID_FILE"
}

PRECMD="start_pre"
ARGS="-D -y /opt/etc/$PROCS.conf -g $PID_FILE"
PREARGS=""

. /opt/etc/init.d/rc.func
